class Bob {

    constructor () {
        var options = {
            isStatic = false,
            restitution = 0.3,
            friction = 0.5,
            densitity = 1.2
        }

        this.body = Bodies.elipse(x, y, 10, options);
        this.radius = 10;

    }






}